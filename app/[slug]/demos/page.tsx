export default function DemosPage() {
  return (
    <main style={{ backgroundColor: 'var(--bg)', minHeight: '100vh' }}>
      <div className="px-4 pt-6">
        <h1 className="text-[28px] font-bold" style={{ color: 'var(--ink)' }}>Démos jouables</h1>
        <p className="mt-4 text-[15px]" style={{ color: 'var(--muted)' }}>Bientôt disponible</p>
      </div>
    </main>
  )
}
